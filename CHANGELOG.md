# Changelog

すべての変更はこのファイルに記録します。

形式は [Keep a Changelog](https://keepachangelog.com/en/1.0.0/) に従い、バージョン管理は [Semantic Versioning](https://semver.org/spec/v2.0.0.html) に準拠します。

## [0.2.0] - 未リリース

0.1.0（旧名 VRC Avatar Color Changer / VACC）からの変更です。

### 重要な変更

- **名前を「いろか（Iroca）」に変えました。** メニューは `Tools > いろか` です。旧版（`Assets/VACC` フォルダ）とは別物として入るため、0.1.0 を使っていた場合は `Assets/VACC` を削除してください（プロジェクト内に保存したプリセットが必要なら、削除の前にバックアップしてください）
- **導入方法が変わりました。** unitypackage で配るのは、インポートすると最新版をダウンロードして入れる `Iroca_Installer.unitypackage`（[VPMPackageAutoInstaller](https://github.com/anatawa12/VPMPackageAutoInstaller) 製）です。いろか本体はプロジェクトの `Packages` に入り、以後は VCC / ALCOM から更新できます。VCC / ALCOM にリポジトリ `https://yukkuri-aoba.github.io/Iroca/index.json` を追加して直接導入することもできます
- **再着色アルゴリズムを OkLab（知覚的な色空間）へ刷新しました。** 色ムラ・ベタ塗り化・色破壊を解消し、陰影や模様をより自然に保ったまま色を変更できます
- **ライセンスを「PolyForm Noncommercial License 1.0.0」から [PolyForm Shield License 1.0.0](LICENSE) に変更しました。** 商用利用と、改変・再配布ができるようになりました。本ツールと競合する製品・サービスの提供には使えません

### 追加

- 自動調整: スポイトで色を取ったパーツについて、暗部からハイライトまでを覆うように許容範囲などを自動で合わせます
  - クリックした位置の AI マスク提案を手がかりに使うため、Unity Sentis とモデルファイルが必要です（未導入のときは中止して導入を案内します）
  - ゾーンカード上に、スポイト位置があるか（AI 提案を参照できるか）を表示します
- AI マスク提案（実験的）: プレビュー上のパーツを右クリック（mac は Control+クリック）すると AI（MobileSAM）が領域を推定し、その場で編集対象のマスク（除外または含める）へ追加します
  - 使うには Unity Sentis（`com.unity.sentis` 2.1.3。Unity 2022.3.11f1 以降）とモデルファイル（約 44 MB）が必要です。導入・モデルのダウンロード（sha256 検証つき）・再起動は、ウィンドウ上部の帯から行えます。モデルは手動配置にも対応します
  - 1 回の右クリックで取れるのは、つながった 1 つの領域（UV アイランド）です。複数の島に分かれたパーツは島ごとにクリックします
  - 右クリック 1 回で即マスクへ反映され、確定ボタンはありません。間違えたら Ctrl+Z で 1 つずつ戻せます。追加後は通常のマスクとして編集できます
  - 粒度（自動／細かい／大きい）を選べます。推定を待つ間もプレビューを動かせます
  - MobileSAM（Apache License 2.0）を ONNX 形式に変換して使用しています
- 含めるマスク: 塗った領域を「必ず色替えに含める」ゾーン別マスクを追加しました。色の判定で拾いきれなかった部分（強い光沢、離れた場所にある同色パーツの取りこぼしなど）をブラシまたは AI マスク提案で追加できます
  - 除外マスクと重なった画素は除外が優先されます
  - 追加した領域は、ゾーンの色マッチした部分から推定した素材の基準を使って「同素材として」色替えされます
- ゾーン別の除外マスク: 共通マスクとゾーン別マスクを組み合わせられます。マスクはゾーンごとに異なる色で重ね表示し、プリセットへの同梱保存・読み込み（ON/OFF 切り替え可）に対応します
- マスク編集ウィンドウ: 編集対象のゾーン、マスクの種類（除外／含める）、ツール（塗る／消す／AI 提案）、ツール別の設定、取り消し、クリアを 1 つのウィンドウにまとめました。プレビューの隣に置いたまま作業できます
- マスクデータのファイル永続化: エディタを再起動してもマスクが残ります。テクスチャを削除すると、対応するマスクデータも削除します
- 連続領域モード: クリックした地点とつながった同色領域だけを対象にできます。ゾーンの「シード (任意)」行の「指定」ボタンから、プレビューをクリックしてシード地点を指定できます
- 境界クリーンアップ（α分解）: アンチエイリアス境界のハロー（色のにじみ）を構造的に除去します（既定で有効。近傍半径は加工設定の「詳細設定」で調整できます）
- ゾーンのソロ表示: そのゾーンだけをプレビューして、実際にどこを拾っているかを確かめられます（表示だけの機能で、保存内容は変わりません）
- プレビュー: 操作モード（スポイト／シード指定／マスクを塗る・消す／AI 提案）を 1 行で表示し、Esc キーでどのモードも解除できます。ズームの「リセット」、押している間だけ変更前を表示する「元を表示」、シード位置とスポイト位置の目印を追加しました
- どのモードでもプレビューを動かせます: 中ボタンドラッグと Alt+ドラッグがパンです（マスクを塗っている最中も使えます）
- エクスポートの「Project で表示」: 直前に保存したテクスチャを Project ウィンドウで選択・表示します
- 手順の案内: 主要な手順に番号を付け、済んだ手順に ✓ を付けて「次に何をするか」を 1 行で案内します
- ゾーン詳細パラメータのリセット: 色・許容範囲・名前を保持したまま、詳細パラメータだけを既定値に戻せます

### 変更

- ゾーンの優先度を、レイヤーインデックスからリストの並び順（ドラッグ＆ドロップで並び替え）に変えました。重なり部分は上位のゾーンだけが適用されます
- 「アドバンスモード」を廃止し、細かいパラメータはゾーンと加工設定それぞれの「詳細設定」折りたたみにまとめました。ゾーンの詳細パラメータは役割ごとの小見出し（選択の範囲／ハイライト／暗部・無彩色／色の写り方／マッチング距離の重み）で整理しています
- 新規ゾーンの許容範囲は 0.20 で始まり、色を指定すればすぐプレビューが変わります。既定名は「ゾーン 1」「ゾーン 2」…の連番です
- 完了・エラーのダイアログを通知に置き換え、適用＆保存と自動調整を非同期化しました。エクスポートの失敗は、閉じるまで消えない表示としてエクスポート欄に残ります
- Unity Undo を統合しました。スライダー・トグル・マスクのストローク・ゾーン操作・プリセット読み込みを Ctrl+Z / Ctrl+Y で取り消し・やり直しできます
- ブラシカーソルの色が、塗ろうとしているマスクの種類（赤＝除外／緑＝含める）を表します。消しゴムは白です
- 一括適用・UV 矩形モードは実装継続中のため、当面 UI から非表示にしています

### パフォーマンス

- 色処理とマスク生成のマルチスレッド化、後段パス（穴埋め・境界回復・ブラー）の変更領域への限定、OkLab 変換の高速化、マスクのビットパック化、エクスポートの非同期化などで、プレビューと書き出しを高速化しました

### 修正

- 色マッチング精度の向上: 低彩度サンプルや無彩色（白・黒・グレー）での選択精度を改善し、隣接する似た色や白背景への巻き込み・誤検出を大幅に低減しました
- 再着色品質の改善: 明部の白飛び・段差、エッジの不自然なフチ、極端なターゲット色でのバンディングなどを修正しました
- マスクの重ね表示: 拡大したときに境界がぼけて実際より広く見える問題、画像の端に接したマスクが薄れて見える問題を修正しました
- UI 要素が長くなると隠れて操作できなくなる問題（エクスポートボタンの見切れを含む）を修正しました
- 高解像度プレビューの上下反転・スクロールずれ、ズーム / パン操作の感度を修正しました
- テクスチャ切り替え時に古いマスクの重ね表示が残る問題を修正しました
- 新しく生成したテクスチャが、元のテクスチャの読み込み設定を引き継ぐようにしました
- テクスチャの Read/Write 設定を変更する前に確認ダイアログを表示するようにしました
- 言語設定がエディタ再起動後も維持されるようにしました
- 有効なゾーンが無いまま保存すると、無変更のファイルが「完了」表示になる問題を修正しました

### ドキュメント

- オンラインマニュアル（日本語・英語）を用意しました。設定の効き方をスライダーで試せます

---

## [0.1.0] - 2026-04-18 (Beta)

### 追加

- カラーゾーンによるテクスチャ色改変（カラーピック / UV 矩形 モード）
- 模様保持スライダー、エッジ柔らかさ調整、彩度制限、ハイライト補助
- レイヤーインデックスによる優先度制御
- エッジぼかし / AA 境界クリーンアップ
- 除外マスク（ブラシによる保護エリア描画、最大 30 ステップ Undo 対応）
- プレビュー（ズーム、前後比較、差分表示、高ズーム時の詳細プレビュー）
- プリセットの保存・読み込み（プロジェクト内 / ユーザー共通）、JSON の書き出し・読み込み
- バッチ適用（複数テクスチャへの同時適用）
- 詳細設定モード（内部設定値の微調整）
- 日本語・英語対応（自動判定）
