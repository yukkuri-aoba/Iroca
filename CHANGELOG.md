# Changelog

すべての変更はこのファイルに記録します。

形式は [Keep a Changelog](https://keepachangelog.com/en/1.0.0/) に従い、バージョン管理は [Semantic Versioning](https://semver.org/spec/v2.0.0.html) に準拠します。

## [0.2.0] - 2026-05-14

### 追加

- **カラーゾーンごとの除外マスク**: 共通マスクとゾーン別マスクを組み合わせた構成をサポートしました
  - ColorZone に GUID ベースの id フィールドを追加しました
  - SessionState に MaskIndex と各対象マスクを分割して保存します
  - 旧来の単一マスクフォーマットから自動で移行します
  - プリセットへのマスク同梱保存・読み込みに対応しました（ON/OFF 切り替え可）
  - ゾーンマスクのオーバーレイを HSV 黄金比色で描画します
  - 各ゾーン行に「編集対象」トグルボタンを追加しました
- **AA 境界の色汚染除去（境界クリーンアップ・α 分解）**: アンチエイリアス境界のハロー効果を構造的に除去します
  - `DecontaminateAaBoundary` と `BoxFilterSum` による局所背景推定と α 逆算を実装しました
  - `useDecontamination` トグル（デフォルト有効）を UI に追加しました
  - `decontaminationRadius` スライダー（詳細設定モード）を追加しました
  - プリセットへの保存・復元に対応しました
- **Unity Undo の完全統合**: スライダー、トグル、マスクのストローク、ゾーン操作がすべて Unity 標準の Ctrl+Z / Ctrl+Y で取り消し・やり直し可能になりました。独自の Ctrl+Z キャプチャは廃止しました
- **マスクデータのファイル永続化**: マスクキャッシュを `UserSettings/Camereo/MaskCache/` に保存します。エディタを再起動しても保持されます
- **テクスチャ削除時のマスクキャッシュ自動クリーンアップ**: テクスチャを削除すると、対応するマスクファイルを自動で削除します

### 修正

- **低彩度サンプルでの色マッチング精度向上**: RGB-HSV ハイブリッド距離とハイライト色相キャップを導入しました
  - サンプル色の彩度が低い場合（S < 0.15）に RGB ユークリッド距離と HSV 距離を彩度に応じて線形ブレンドします
  - ハイライト補助では色相距離の上限を制限し、低彩度の隣接色への誤一致を防ぎます
- **ColorZone の id フィールドのコンパイルエラー修正**: マスク機能追加時のコミット漏れを修正しました
- **ハイライトの色変換調整**
- **UI の可視性問題の修正**: UI 要素が長くなると隠れて操作できなくなる問題を解決しました
- **テクスチャ読み込み設定の継承**: 新しく生成されたテクスチャが元のテクスチャの読み込み設定を引き継ぐようになりました
- **詳細プレビューのゾーンオーバーレイ表示順の修正**: 詳細プレビューのゾーンオーバーレイ優先順位がメインプレビューと一致しない問題を修正しました
- **ゾーン専用マスクの誤削除修正**: `EnsureMasks` 実行時にゾーン専用マスクが誤って削除される問題を修正しました
- **Read/Write 有効化前の確認ダイアログ追加**: テクスチャの Read/Write 設定を変更する前に確認ダイアログを表示するようになりました
- **言語設定の永続化**: 言語設定を EditorPrefs に保存し、エディタ再起動後も設定が維持されます
- **マスクキャッシュ掃除の誤判定修正**: `*.vacc-mask.json` のファイル名から GUID を正しく抽出するよう修正し、正常な MaskCache が孤児扱いで削除される問題を解消しました
- **テクスチャ切り替え時のマスクオーバーレイ残留修正**: 同サイズのテクスチャへ切り替えた際に古いマスクオーバーレイが残る問題を解消しました
- **一括適用の例外耐性改善**: 個別テクスチャの処理に失敗しても一時 `Texture2D` を確実に破棄し、残りの処理を継続するよう改善しました
- **エディタ無効化時の一時リソース解放改善**: プレビュー・詳細プレビュー・マスクオーバーレイの非同期処理と一時テクスチャを、ウィンドウ無効化時に確実に停止・解放するよう整理しました
- **Flood Fill シード入力の対象ゾーン修正**: 再公開時にシード入力が複数ゾーンへ一括反映されないようローカル化しました

### 変更

- **大規模リファクタリング**: コードを `Core/` / `Infra/` / `UI/` の 3 層構造に再編しました。`CamereoWindow` を複数のサブビューに分割し、保守性を向上しました
- **ColorZone の計算処理リファクタリング**: メソッドに分割し、事前計算をキャッシュ化してパフォーマンスを最適化しました
- **未完成機能を一時的に非表示**: 一括適用 UI と連続領域モード（Flood Fill）の UI・処理本体を、実装継続中につき今リリースでは無効化しました
- **ライセンスを変更**: 「PolyForm Noncommercial License 1.0.0」から [PolyForm Shield License 1.0.0](LICENSE) に変更しました
  - 商用利用が可能になりました
  - 改変および再配布が自由に許可されるようになりました
  - 制限：競合製品の提供に使用することはできません

### 改善

- **マルチスレッド化によるパフォーマンス向上**: 色処理とマスク生成の並列化により、プレビューの処理速度を大幅に改善しました

---

## [0.1.0] - 2026-04-18 (Beta)

### 追加

- カラーゾーンによるテクスチャ色改変（カラーピック / UV 矩形 モード）
- 模様保持スライダー、エッジ柔らかさ調整、彩度制限、ハイライト補助
- レイヤーインデックスによる優先度制御
- エッジぼかし / AA 境界クリーンアップ
- 除外マスク（ブラシによる保護エリア描画、最大 30 ステップ Undo 対応）
- プレビュー（ズーム、前後比較、差分表示、高ズーム時の詳細プレビュー）
- プリセットの保存・読み込み（プロジェクト内 / ユーザー共通）、JSON の書き出し・読み込み
- バッチ適用（複数テクスチャへの同時適用）
- 詳細設定モード（内部設定値の微調整）
- 日本語・英語対応（自動判定）
