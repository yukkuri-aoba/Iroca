# Changelog

すべての変更はこのファイルに記録します。

形式は [Keep a Changelog](https://keepachangelog.com/en/1.0.0/) に従い、バージョン管理は [Semantic Versioning](https://semver.org/spec/v2.0.0.html) に準拠します。

## [未リリース]

### 追加

- AI マスク提案（実験的・任意インストール）: プレビュー上のパーツをクリックすると AI（MobileSAM）が領域を推定して提案し、積み上げて除外マスクへ確定できます
  - Unity Sentis（`com.unity.sentis` 2.1.3）とモデルファイルを導入した場合のみ表示されます。未導入時は従来と完全に同一の動作です
  - モデルは UI からの自動ダウンロード（sha256 検証つき）または手動配置に対応します
  - 提案の追加・1つ戻す・クリア・「選択を除外に追加」「選択以外を除外に追加」の確定操作に対応し、確定後は通常のマスクとして編集できます
  - MobileSAM（Apache License 2.0）を ONNX 形式に変換して使用しています

## [0.2.0] - 2026-06-25

### 追加

- カラーゾーンごとの除外マスク: 共通マスクとゾーン別マスクを組み合わせた構成をサポートしました
  - ゾーンマスクのオーバーレイをゾーンごとに異なる色で描画します
  - 各ゾーン行に「編集対象」トグルボタンを追加しました
  - プリセットへのマスク同梱保存・読み込みに対応しました（ON/OFF 切り替え可）
  - 以前のバージョンのマスクデータから自動で移行します
- マスクデータのファイル永続化: エディタを再起動してもマスクデータが維持されます。テクスチャを削除すると、対応するマスクデータを自動で削除します
- 連続領域モード: クリックした地点とつながった同色領域だけを対象にできるモードを追加しました（連結成分アンカリング）
  - 既定は自動アンカリングで、Shift+クリックで任意のシード地点を指定できます
  - 詳細プレビューが最終出力と完全に一致します
- 境界クリーンアップ（α分解）: アンチエイリアス境界のハロー（色のにじみ）を構造的に除去します
  - 「境界クリーンアップ（α分解）」トグル（デフォルト有効）を追加しました
  - 上級モードに「α分解 近傍半径」スライダーを追加しました
  - プリセットへの保存・復元に対応しました
- ワークフローガイド: 主要な手順に番号を付け、初回起動時に操作の流れを示す導入ヒントを表示します
- ゾーン詳細パラメータのリセット: 色・許容範囲・名前を保持したまま、詳細パラメータだけを既定値に戻すボタンを追加しました

### 変更

- ゾーンの優先度をリストの並び順で制御するようにしました。重なり部分は上位のゾーンのみが適用され、ドラッグ＆ドロップで並び替えできます（並び替え中はマウス追従のゴーストを表示）
- 再着色アルゴリズムを OkLab（知覚的な色空間）へ刷新しました。色ムラ・ベタ塗り化・色破壊を解消し、陰影や模様をより自然に保ったまま色を変更できます
- 編集モードを「通常 / 上級」の段階表示に整理しました。通常モードでは主要な設定だけを表示し、上級モードで内部パラメータまで開きます
- 完了・エラーのモーダルダイアログを非モーダル通知に置き換え、適用＆保存と自動調整を非同期化しました。処理中もウィンドウがブロックされず、ファイル I/O の失敗も通知で可視化します
- Unity Undo を完全統合しました。スライダー・トグル・マスクのストローク・ゾーン操作・プリセット読み込みが、Unity 標準の Ctrl+Z / Ctrl+Y で取り消し・やり直しできます
- 内部コードを保守性向上のためリファクタリングし、メインウィンドウを複数のビューに分割しました
- ライセンスを「PolyForm Noncommercial License 1.0.0」から [PolyForm Shield License 1.0.0](LICENSE) に変更しました
  - 商用利用が可能になりました
  - 改変および再配布が自由に許可されるようになりました
  - 制限：競合する製品・サービスの提供に使用することはできません
- 一括適用・UV 矩形モードは実装継続中のため、当面 UI から非表示にしています

### パフォーマンス

- 色処理とマスク生成のマルチスレッド化により、プレビューの処理速度を大幅に改善しました
- 後段パス（穴埋め・境界回復・ブラー）を変更領域に限定し、小さなマッチでの処理を高速化しました
- OkLab 変換の高速化・マスクのビットパック化・エクスポートの非同期化など、全体的な最適化を行いました

### 修正

- 色マッチング精度の向上: 低彩度サンプルや無彩色（白・黒・グレー）での選択精度を改善し、隣接する似た色や白背景への巻き込み・誤検出を大幅に低減しました
- 再着色品質の改善: 明部の白飛び・段差、エッジの不自然なフチ、極端なターゲット色でのバンディングなどを修正し、出力をより忠実にしました
- ハイライト（明部）の色変換を調整しました
- UI 要素が長くなると隠れて操作できなくなる問題を解決しました（エクスポートボタンの見切れを含む）
- 高解像度プレビューの上下反転・スクロールずれ、ズーム / パン操作の感度を修正しました
- 詳細プレビューのゾーンオーバーレイ表示順がメインプレビューと一致しない問題を修正しました
- ゾーン専用マスクや正常なマスクデータが誤って削除される問題を修正しました
- テクスチャ切り替え時に古いマスクオーバーレイが残る問題を修正しました
- 新しく生成されたテクスチャが元のテクスチャの読み込み設定を引き継ぐようにしました
- テクスチャの Read/Write 設定を変更する前に確認ダイアログを表示するようにしました
- 言語設定がエディタ再起動後も維持されるようにしました
- 一括処理で個別テクスチャの処理に失敗しても、一時リソースを確実に解放して残りの処理を継続するよう改善しました
- ウィンドウを閉じた際に確実にリソースを解放するよう改善しました
- 有効なゾーンが無いまま保存して無変更ファイルが「完了」表示になる問題を修正しました
- 連続領域モードのシード入力が対象ゾーン以外にも反映される問題を修正しました

---

## [0.1.0] - 2026-04-18 (Beta)

### 追加

- カラーゾーンによるテクスチャ色改変（カラーピック / UV 矩形 モード）
- 模様保持スライダー、エッジ柔らかさ調整、彩度制限、ハイライト補助
- レイヤーインデックスによる優先度制御
- エッジぼかし / AA 境界クリーンアップ
- 除外マスク（ブラシによる保護エリア描画、最大 30 ステップ Undo 対応）
- プレビュー（ズーム、前後比較、差分表示、高ズーム時の詳細プレビュー）
- プリセットの保存・読み込み（プロジェクト内 / ユーザー共通）、JSON の書き出し・読み込み
- バッチ適用（複数テクスチャへの同時適用）
- 詳細設定モード（内部設定値の微調整）
- 日本語・英語対応（自動判定）
