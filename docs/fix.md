- ズームすると高解像度のテクスチャプレビューを表示する機能があったはずだが壊れている(大きく動かすと隠れていた通常の解像度の物が見えてしまう)
- プレビューが実際の結果と異なることがある
- プレビュー生成中...の表示がプレビュー領域内に表示されたり、普通に枠外に表示されたりするので枠外表示に統一
- クレジット部分のライセンスが古いまま
- テクスチャを生成するボタンと、元のパスを開くボタンがウィンドウからはみだして押せない
- エクスポートセクションの順番が気に食わない(チェックボックスを上側にまとめ、ファイル名を一番下にしてほしい)