# 基本ルール

## 応答
- ユーザーへの応答は**日本語**で書く。

## UI 実装方針
- Unity Editor UI に新しい操作要素（ボタン・スライダー・トグル・入力フィールド等）を追加するときは、**必ずツールチップを付ける**（`GUIContent(..., tooltip)` など）。操作の意味と効果が UI だけで伝わるようにするため。
- 既存要素の文言・レイアウトを変えるだけのときは対象外。
